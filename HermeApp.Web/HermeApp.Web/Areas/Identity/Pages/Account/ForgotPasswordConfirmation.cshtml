@page
@model ForgotPasswordConfirmation
@{
    ViewData["Title"] = "Forgot password confirmation";
}

<div class="div-semi-centre">
    <div class="col-md-4">
        <div class="default-title">
            <h1>@ViewData["Title"]</h1>
            <p>
                Please check your email to reset your password.
            </p>
            <div class="button">
                <a asp-page="./Login" > <button id="Register" type="button" class="w-100 btn btn-lg btn-primary">To Login</button></a>
            </div>
        </div>
    </div>
</div>
